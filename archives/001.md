# TASK-001: Initial Monorepo Package Setup

**Status:** Pending
**Requested By:** Hrishi Joshi
**Priority:** High

---

## 🎯 Objective
Initialize a new shared utility package within the monorepo to handle common string manipulation and validation logic that will be used across future services.

## 📋 Requirements
1. **Location:** Create a new directory at `packages/shared-utils`.
2. **Language:** Use TypeScript.
3. **Features:**
    - Implement a `validateEmail` function using regex.
    - Implement a `slugify` function for URL-friendly strings.
    - Export these via a main index file.
4. **Testing:** - Add Vitest or Jest for unit testing.
    - Ensure 100% test coverage for this specific utility package.
5. **Documentation:** - Include a local `README.md` within the package folder explaining how to use the utilities.

## 🛡️ Guardrail Compliance
- **Performance:** Ensure regex patterns are optimized to avoid ReDoS (Regular Expression Denial of Service).
- **Technology:** Must be compatible with the existing `pnpm` or `npm` workspace configuration.

---

## 🤖 Agent Instructions
1. Parse this file and begin implementation in a feature branch.
2. Run tests and security scans locally.
3. Upon successful merge to `main`, **move this file** to the `/archives` directory.
